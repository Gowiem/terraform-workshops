#!/bin/bash

# You will need a provisioner script to install nginx 
# and start up the server.
#
# apt-get is the command to install a package on Ubuntu
#
# You'll also need the systemctl command to start the server

